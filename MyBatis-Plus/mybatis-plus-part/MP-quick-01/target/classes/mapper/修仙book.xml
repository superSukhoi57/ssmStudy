<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.Mapper.ikunMapper">

    <select id="queryByPrice" resultType="com.example.pojo.修仙book">
        select * from 修仙book where price>#{price}
    </select>

</mapper>